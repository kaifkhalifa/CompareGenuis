import mockProducts from './mockProducts';
import mockProductsPart2 from './mockProductsPart2';

// Combine all products into one array
const allProducts = [...mockProducts, ...mockProductsPart2];

export { mockProducts, mockProductsPart2, allProducts };
